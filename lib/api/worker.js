'use strict';

/**
 * The `Worker` class provides the common API shared by all workers.
 *
 * Every worker should extend this base class.
 *
 */
class Worker {

    run(args) {

    }
}

module.exports = Worker;